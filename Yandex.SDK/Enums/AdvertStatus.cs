public enum AdResult {
    OK,
    Delay,
    Error,
    Offline
}

public enum RewardedAdResult
{
    Rewarded,
    Closed,
    Error
}

public enum StickyBannerStatus
{
    NotShowing,
    Showing,
    AdvIsNotConnected,
    Unknown
}
